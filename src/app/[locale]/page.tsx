/* src/app/[locale]/page.tsx */
export default function Home() {
  // El layout en src/app/[locale]/layout.tsx ya incluye Navbar y Footer.
  // Dejamos la portada sin contenido adicional.
  return <main />;
}